# Local README rendering artifacts (used by tooling to preview the
# README in browser-rendered light/dark, not part of the published profile).
.preview/

# Visual brainstorming companion (superpowers) — local mockups, not published.
.superpowers/

# Headless preview / screenshot artifacts (local visual checks only).
.playwright-mcp/
.preview-*.html
/*.png

# OS noise
.DS_Store
*.swp
